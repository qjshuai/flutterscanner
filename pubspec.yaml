name: scanner
description: qr scanner

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.3+5

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_slidable: ^0.5.7
  cupertino_icons: ^1.0.0
  shared_preferences: ^0.5.10
  intl:
  intl_translation:
  flutter_localized_locales:
  equatable:
  fluttertoast: ^7.1.1
  provider: ^4.3.2+2
  auto_size_text: ^2.1.0
  get: ^3.10.2
  flutter_screenutil: ^3.2.0
  animations: ^1.1.2
  get_it: ^5.0.1
  pull_to_refresh: ^1.6.2
  environment:
    path: /Users/qjs/Code/Projects/flutter/universal_cotte/packages/environment
  supercharged: ^1.10.0
  quiver:
  cached_network_image:
  sqflite: ^1.3.0
  flutter_local_notifications:
  json_annotation: ^3.0.1
  package_info: ^0.4.0
  url_launcher:

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0
  build_runner: ^1.10.1
  json_serializable: ^3.4.1
  flutter_launcher_icons: ^0.8.1

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/icon.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/